#!/usr/bin/env ruby

if $0 == __FILE__
  $:.unshift File.expand_path('../../lib', __FILE__)
  $:.unshift File.expand_path('../../vendor', __FILE__)
  require 'rubygems'
end

require 'kicker'
Kicker.run
